server:
  port: 8081

spring:
    application:
        name: Web-Api-Michael
    datasource:
        url: jdbc:h2:mem:webapidb
        driverClassName: org.h2.Driver
        username: webapi
        password: klasd456!.sdf
    h2:
        console:
            enabled: true
            path: /h2-console
    jpa:
        database-platform: org.hibernate.dialect.H2Dialect
        defer-datasource-initialization: true
        show-sql: true
        hibernate:
            ddl-auto: create-drop


